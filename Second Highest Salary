with
    a as (
        select
            salary
        from
            employee
        order by
            salary desc
        limit
            2
    )
select
    *
from
    a
where
    salary <> (
        select
            max(salary)
        from
            employee
    )